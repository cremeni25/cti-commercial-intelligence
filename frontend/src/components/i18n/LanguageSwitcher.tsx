"use client"

/**
 * O seletor multilíngue está temporariamente desativado.
 * CTI Web e APP CRM operam exclusivamente em PT-BR até nova homologação explícita.
 */
export default function LanguageSwitcher(props: { compact?: boolean }) {
  void props
  return null
}
